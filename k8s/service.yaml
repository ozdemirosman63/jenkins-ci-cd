apiVersion: v1
kind: Service
metadata:
  name: project4-service
spec:
  type: NodePort
  selector:
    app: project4  # 🟢 Pod label'ı ile birebir aynı olmalı
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080